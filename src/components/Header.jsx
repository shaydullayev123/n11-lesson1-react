import { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header>
        <a href="#">Logo</a>
        <span>Header</span>
        <button>Login</button>
      </header>
    );
  }
}

export default Header
